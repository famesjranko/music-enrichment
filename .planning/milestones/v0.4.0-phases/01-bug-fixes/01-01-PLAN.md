---
phase: 01-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]

must_haves:
  truths:
    - "MusicBrainz empty search results return NotFound, not RateLimited"
    - "Last.fm API calls use HTTPS, not HTTP"
    - "Last.fm does not advertise TRACK_POPULARITY in its capabilities"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt"
      provides: "Corrected empty-result handling"
      contains: "EnrichmentResult.NotFound(type, id)"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt"
      provides: "HTTPS base URL"
      contains: "https://ws.audioscrobbler.com/2.0/"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt"
      provides: "No TRACK_POPULARITY capability"
  key_links:
    - from: "MusicBrainzProvider.enrichAlbum"
      to: "EnrichmentResult.NotFound"
      via: "empty search results"
      pattern: "if \\(releases.isEmpty\\(\\)\\) return EnrichmentResult\\.NotFound"
    - from: "LastFmApi.buildUrl"
      to: "BASE_URL"
      via: "string interpolation"
      pattern: "https://ws.audioscrobbler"
---

<objective>
Fix MusicBrainz empty-result misclassification (BUG-01), Last.fm HTTP-to-HTTPS (BUG-02), and Last.fm TRACK_POPULARITY capability removal (BUG-03).

Purpose: Correct three provider bugs so that empty search results are not misreported as rate limiting, all API traffic uses HTTPS, and Last.fm does not falsely claim to provide track-level popularity data.
Output: Three corrected provider files with passing unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix MusicBrainz empty results returning RateLimited (BUG-01)</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/testutil/FakeHttpClient.kt
  </read_first>
  <behavior>
    - Test: `empty album search results return NotFound` -- Given: FakeHttpClient returns `{"releases":[]}` for "release?query". When: enrich(ForAlbum, GENRE). Then: result is EnrichmentResult.NotFound.
    - Test: `empty artist search results return NotFound` -- Given: FakeHttpClient returns `{"artists":[]}` for "artist?query". When: enrich(ForArtist, GENRE). Then: result is EnrichmentResult.NotFound.
    - Test: `empty recording search results return NotFound` -- Given: FakeHttpClient returns `{"recordings":[]}` for "recording?query". When: enrich(ForTrack, GENRE). Then: result is EnrichmentResult.NotFound.
  </behavior>
  <action>
    1. In MusicBrainzProviderTest.kt, add three new tests (backtick names, Given-When-Then comments):
       - `empty album search results return NotFound`: configure `httpClient.givenJsonResponse("release?query", """{"releases":[]}""")`, call `provider.enrich(EnrichmentRequest.forAlbum("Nothing", "Nobody"), EnrichmentType.GENRE)`, assert `result is EnrichmentResult.NotFound`.
       - `empty artist search results return NotFound`: configure `httpClient.givenJsonResponse("artist?query", """{"artists":[]}""")`, call `provider.enrich(EnrichmentRequest.forArtist("Nobody"), EnrichmentType.GENRE)`, assert `result is EnrichmentResult.NotFound`.
       - `empty recording search results return NotFound`: configure `httpClient.givenJsonResponse("recording?query", """{"recordings":[]}""")`, call `provider.enrich(EnrichmentRequest.forTrack("Nothing", "Nobody"), EnrichmentType.GENRE)`, assert `result is EnrichmentResult.NotFound`.
    2. Run tests -- they should FAIL (currently returns RateLimited).
    3. In MusicBrainzProvider.kt, change three lines:
       - Line 129: `if (releases.isEmpty()) return EnrichmentResult.RateLimited(type, id)` -> `if (releases.isEmpty()) return EnrichmentResult.NotFound(type, id)`
       - Line 170: `if (artists.isEmpty()) return EnrichmentResult.RateLimited(type, id)` -> `if (artists.isEmpty()) return EnrichmentResult.NotFound(type, id)`
       - Line 200: `if (recordings.isEmpty()) return EnrichmentResult.RateLimited(type, id)` -> `if (recordings.isEmpty()) return EnrichmentResult.NotFound(type, id)`
    4. Run tests -- they should PASS.
    5. Also update the existing test `enrich returns RateLimited on null response` -- note this test configures NO response (FakeHttpClient returns null), which causes MusicBrainzApi to return emptyList(). With the fix, this test will now FAIL. Update its assertion to expect NotFound instead of RateLimited, and rename it to `enrich returns NotFound on null response`. The underlying issue is that the API layer conflates null-response with empty-results (both return emptyList()), so both cases now correctly map to NotFound at the provider level.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzProviderTest" --no-daemon -q</automated>
  </verify>
  <acceptance_criteria>
    - MusicBrainzProvider.kt contains zero occurrences of `EnrichmentResult.RateLimited`
    - MusicBrainzProvider.kt line 129 (enrichAlbum empty check) reads `return EnrichmentResult.NotFound(type, id)`
    - MusicBrainzProvider.kt line 170 (enrichArtist empty check) reads `return EnrichmentResult.NotFound(type, id)`
    - MusicBrainzProvider.kt line 200 (enrichTrack empty check) reads `return EnrichmentResult.NotFound(type, id)`
    - MusicBrainzProviderTest.kt contains test `empty album search results return NotFound`
    - MusicBrainzProviderTest.kt contains test `empty artist search results return NotFound`
    - MusicBrainzProviderTest.kt contains test `empty recording search results return NotFound`
    - All MusicBrainzProviderTest tests pass
  </acceptance_criteria>
  <done>MusicBrainz empty search results return NotFound on all three request types (album, artist, track), verified by 3 new unit tests and all existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Last.fm HTTPS and remove TRACK_POPULARITY (BUG-02, BUG-03)</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/testutil/FakeHttpClient.kt
  </read_first>
  <behavior>
    - Test (BUG-02): `API calls use HTTPS` -- Given: provider is initialized. When: any API call is made. Then: FakeHttpClient.requestedUrls all start with "https://".
    - Test (BUG-03): `capabilities do not include TRACK_POPULARITY` -- Given: provider instance. When: checking capabilities. Then: none have type == EnrichmentType.TRACK_POPULARITY.
    - Test (BUG-03): `enrich returns NotFound for TRACK_POPULARITY` -- Given: valid provider with artist info response configured. When: enrich(ForArtist, TRACK_POPULARITY). Then: result is EnrichmentResult.NotFound.
  </behavior>
  <action>
    1. In LastFmProviderTest.kt, add three new tests:
       - `API calls use HTTPS`: configure `httpClient.givenJsonResponse("artist.getinfo", ARTIST_INFO_JSON)`, call `provider.enrich(EnrichmentRequest.forArtist("Radiohead"), EnrichmentType.GENRE)`, assert `httpClient.requestedUrls.all { it.startsWith("https://") }`.
       - `capabilities do not include TRACK_POPULARITY`: assert `provider.capabilities.none { it.type == EnrichmentType.TRACK_POPULARITY }`.
       - `enrich returns NotFound for TRACK_POPULARITY`: configure `httpClient.givenJsonResponse("artist.getinfo", ARTIST_INFO_JSON)`, call `provider.enrich(EnrichmentRequest.forArtist("Radiohead"), EnrichmentType.TRACK_POPULARITY)`, assert `result is EnrichmentResult.NotFound`.
    2. Run tests -- HTTPS test and TRACK_POPULARITY capability test should FAIL.
    3. In LastFmApi.kt, change line 80: `const val BASE_URL = "http://ws.audioscrobbler.com/2.0/"` -> `const val BASE_URL = "https://ws.audioscrobbler.com/2.0/"`
    4. In LastFmProvider.kt, remove the TRACK_POPULARITY capability from the `capabilities` list (delete line 39: `ProviderCapability(EnrichmentType.TRACK_POPULARITY, priority = 50),`).
    5. In LastFmProvider.kt, remove the TRACK_POPULARITY branch from the `when` block in `enrich()` (delete line 56: `EnrichmentType.TRACK_POPULARITY -> enrichPopularity(artistRequest, type)`). The `else` branch already returns NotFound, so TRACK_POPULARITY requests will fall through to NotFound.
    6. Run tests -- all should PASS.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.lastfm.LastFmProviderTest" --no-daemon -q</automated>
  </verify>
  <acceptance_criteria>
    - LastFmApi.kt contains `const val BASE_URL = "https://ws.audioscrobbler.com/2.0/"`
    - LastFmApi.kt does NOT contain `http://ws.audioscrobbler.com`
    - LastFmProvider.kt capabilities list does NOT contain `TRACK_POPULARITY`
    - LastFmProvider.kt when-block does NOT contain `EnrichmentType.TRACK_POPULARITY`
    - LastFmProviderTest.kt contains test `API calls use HTTPS`
    - LastFmProviderTest.kt contains test `capabilities do not include TRACK_POPULARITY`
    - LastFmProviderTest.kt contains test `enrich returns NotFound for TRACK_POPULARITY`
    - All LastFmProviderTest tests pass
  </acceptance_criteria>
  <done>Last.fm BASE_URL uses HTTPS, TRACK_POPULARITY removed from capabilities and enrich routing, verified by 3 new unit tests and all existing tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzProviderTest" --tests "com.landofoz.musicmeta.provider.lastfm.LastFmProviderTest" --no-daemon -q
```
All tests pass with zero failures.
</verification>

<success_criteria>
- MusicBrainzProvider.kt has zero `RateLimited` references; all empty-result branches return NotFound
- LastFmApi.kt BASE_URL is `https://ws.audioscrobbler.com/2.0/`
- LastFmProvider.kt capabilities list has exactly 4 entries (SIMILAR_ARTISTS, GENRE, ARTIST_BIO, ARTIST_POPULARITY) -- no TRACK_POPULARITY
- 6 new unit tests pass across both providers
- All pre-existing tests in both test classes still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes/01-01-SUMMARY.md`
</output>
