---
phase: 02-provider-abstraction
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
autonomous: true
requirements: [ABST-04, ABST-05]

must_haves:
  truths:
    - "Each of the 11 providers has a corresponding *Mapper.kt file"
    - "No provider directly constructs EnrichmentData subclasses inline"
    - "EnrichmentEngine.Builder accepts apiKeys(ApiKeyConfig) and withDefaultProviders() constructs all providers from it"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for MusicBrainz"
      contains: "object MusicBrainzMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Last.fm"
      contains: "object LastFmMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Deezer"
      contains: "object DeezerMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for iTunes"
      contains: "object ITunesMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Discogs"
      contains: "object DiscogsMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Cover Art Archive"
      contains: "object CoverArtArchiveMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Fanart.tv"
      contains: "object FanartTvMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Wikidata"
      contains: "object WikidataMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for Wikipedia"
      contains: "object WikipediaMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for ListenBrainz"
      contains: "object ListenBrainzMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibMapper.kt"
      provides: "DTO-to-EnrichmentData mapping for LRCLIB"
      contains: "object LrcLibMapper"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt"
      provides: "ApiKeyConfig data class"
      contains: "data class ApiKeyConfig"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt"
      provides: "Builder.apiKeys() and Builder.withDefaultProviders()"
      contains: "fun apiKeys"
  key_links:
    - from: "each *Provider.kt"
      to: "corresponding *Mapper.kt"
      via: "Provider calls Mapper functions instead of constructing EnrichmentData inline"
      pattern: "Mapper\\."
    - from: "EnrichmentEngine.Builder.withDefaultProviders"
      to: "ApiKeyConfig"
      via: "reads API keys from config to construct providers"
      pattern: "apiKeyConfig"
---

<objective>
Extract mapper pattern for all 11 providers and add centralized API key configuration with Builder integration.

Purpose: Isolate DTO-to-EnrichmentData mapping so that public API shape changes (e.g., adding fields to Artwork, Metadata) only touch mapper files, not provider logic. Centralize API key configuration so consumers pass one ApiKeyConfig object instead of manually constructing each provider with individual keys.

Output: 11 new *Mapper.kt files, updated providers that delegate to mappers, ApiKeyConfig data class, Builder.apiKeys() and Builder.withDefaultProviders() methods.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-abstraction/02-CONTEXT.md
@.planning/phases/02-provider-abstraction/02-01-SUMMARY.md
@.planning/phases/02-provider-abstraction/02-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs -->

From EnrichmentData.kt (after Plan 02 -- IdentifierResolution removed):
```kotlin
sealed class EnrichmentData {
    data class Artwork(val url: String, val width: Int? = null, val height: Int? = null, val thumbnailUrl: String? = null) : EnrichmentData()
    data class Metadata(val genres: List<String>? = null, val label: String? = null, ...) : EnrichmentData()
    data class Lyrics(val syncedLyrics: String? = null, val plainLyrics: String? = null, val isInstrumental: Boolean = false) : EnrichmentData()
    data class Biography(val text: String, val source: String, val language: String = "en", val thumbnailUrl: String? = null) : EnrichmentData()
    data class SimilarArtists(val artists: List<SimilarArtist>) : EnrichmentData()
    data class Popularity(val listenCount: Long? = null, val listenerCount: Long? = null, val rank: Int? = null, val topTracks: List<PopularTrack>? = null) : EnrichmentData()
}
```

From EnrichmentEngine.kt Builder:
```kotlin
class Builder {
    private val providers = mutableListOf<EnrichmentProvider>()
    private var cache: EnrichmentCache? = null
    private var httpClient: HttpClient? = null
    private var config: EnrichmentConfig = EnrichmentConfig()
    private var logger: EnrichmentLogger = EnrichmentLogger.NoOp

    fun addProvider(provider: EnrichmentProvider) = apply { providers.add(provider) }
    fun cache(cache: EnrichmentCache) = apply { this.cache = cache }
    fun httpClient(client: HttpClient) = apply { this.httpClient = client }
    fun config(config: EnrichmentConfig) = apply { this.config = config }
    fun logger(logger: EnrichmentLogger) = apply { this.logger = logger }
    fun build(): EnrichmentEngine
}
```

Provider constructor patterns (what ApiKeyConfig must feed):
- LastFmProvider(apiKey: String, httpClient, rateLimiter)
- FanartTvProvider(projectKey: String, httpClient, rateLimiter)
- DiscogsProvider(personalToken: String, httpClient, rateLimiter)
- MusicBrainzProvider(httpClient, rateLimiter) -- no key
- All others -- no key

Provider DTO model types (what mappers will consume):
- MusicBrainzRelease, MusicBrainzArtist, MusicBrainzRecording
- LastFmSimilarArtist, LastFmArtistInfo
- DeezerAlbumResult
- ITunesAlbumResult
- DiscogsRelease
- FanartTvArtistImages
- WikipediaSummary
- ListenBrainzRecording
- LrcLibResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 11 mapper files from providers</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibProvider.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  </read_first>
  <action>
Create 11 `*Mapper.kt` files, one per provider. Each is a Kotlin `object` with pure functions that convert provider-specific DTOs to `EnrichmentData` subclasses. Then update each provider to call the mapper instead of constructing EnrichmentData inline.

The mapper pattern is: `object XMapper { fun toArtwork(dto: XModel): EnrichmentData.Artwork = ... }`. Functions are package-internal (no `internal` modifier needed since they're in the same package as the provider).

**Mapper specifications:**

1. **MusicBrainzMapper.kt** (`com.landofoz.musicmeta.provider.musicbrainz`):
   - `fun toAlbumMetadata(release: MusicBrainzRelease): EnrichmentData.Metadata` -- extracts genres, label, releaseDate, releaseType, country, barcode, disambiguation from the release
   - `fun toAlbumIdentifiers(release: MusicBrainzRelease): EnrichmentIdentifiers` -- extracts musicBrainzId, musicBrainzReleaseGroupId
   - `fun toArtistMetadata(artist: MusicBrainzArtist): EnrichmentData.Metadata` -- extracts genres, country, disambiguation, artistType, beginDate, endDate
   - `fun toArtistIdentifiers(artist: MusicBrainzArtist): EnrichmentIdentifiers` -- extracts musicBrainzId, wikidataId, wikipediaTitle
   - `fun toTrackMetadata(recording: MusicBrainzRecording): EnrichmentData.Metadata` -- extracts genres, isrc
   - `fun toTrackIdentifiers(recording: MusicBrainzRecording): EnrichmentIdentifiers` -- extracts musicBrainzId
   - MusicBrainzProvider's `buildAlbumResult` and `buildArtistResult` call these mapper functions.

2. **LastFmMapper.kt** (`com.landofoz.musicmeta.provider.lastfm`):
   - `fun toSimilarArtists(artists: List<LastFmSimilarArtist>): EnrichmentData.SimilarArtists`
   - `fun toGenre(tags: List<String>): EnrichmentData.Metadata`
   - `fun toBiography(bio: String): EnrichmentData.Biography`
   - `fun toPopularity(info: LastFmArtistInfo): EnrichmentData.Popularity`

3. **DeezerMapper.kt** (`com.landofoz.musicmeta.provider.deezer`):
   - `fun toArtwork(result: DeezerAlbumResult): EnrichmentData.Artwork` -- picks best cover URL
   - `fun toSearchCandidate(result: DeezerAlbumResult, providerId: String): SearchCandidate`

4. **ITunesMapper.kt** (`com.landofoz.musicmeta.provider.itunes`):
   - `fun toArtwork(result: ITunesAlbumResult, artworkSize: Int): EnrichmentData.Artwork` -- replaces 100x100bb with size
   - `fun toSearchCandidate(result: ITunesAlbumResult, providerId: String): SearchCandidate`

5. **DiscogsMapper.kt** (`com.landofoz.musicmeta.provider.discogs`):
   - `fun toArtwork(release: DiscogsRelease): EnrichmentData.Artwork?` -- returns null if no coverImage
   - `fun toLabelMetadata(release: DiscogsRelease): EnrichmentData.Metadata?` -- returns null if no label
   - `fun toReleaseTypeMetadata(release: DiscogsRelease): EnrichmentData.Metadata?` -- returns null if no releaseType

6. **CoverArtArchiveMapper.kt** (`com.landofoz.musicmeta.provider.coverartarchive`):
   - `fun toArtwork(url: String, thumbnailUrl: String?): EnrichmentData.Artwork`

7. **FanartTvMapper.kt** (`com.landofoz.musicmeta.provider.fanarttv`):
   - `fun toArtwork(url: String): EnrichmentData.Artwork` -- simple wrapper

8. **WikidataMapper.kt** (`com.landofoz.musicmeta.provider.wikidata`):
   - `fun toArtwork(imageUrl: String): EnrichmentData.Artwork`

9. **WikipediaMapper.kt** (`com.landofoz.musicmeta.provider.wikipedia`):
   - `fun toBiography(summary: WikipediaSummary): EnrichmentData.Biography` -- uses summary.extract and "Wikipedia" source

10. **ListenBrainzMapper.kt** (`com.landofoz.musicmeta.provider.listenbrainz`):
    - `fun toPopularity(tracks: List<ListenBrainzRecording>): EnrichmentData.Popularity`

11. **LrcLibMapper.kt** (`com.landofoz.musicmeta.provider.lrclib`):
    - `fun toLyrics(result: LrcLibResult): EnrichmentData.Lyrics`

After creating each mapper, update the corresponding provider to:
1. Import the mapper
2. Replace inline `EnrichmentData.Xxx(...)` construction with `XMapper.toXxx(dto)`
3. The provider still handles the EnrichmentResult wrapping (Success/NotFound/Error) -- the mapper only produces the data payload

Keep each mapper file small (under 60 lines). Each mapper uses the same package as its provider, so it has access to the model types without additional imports.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test -x :musicmeta-android:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 11 mapper files exist: ls musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/*/\*Mapper.kt shows exactly 11 files
    - Each mapper is an object: grep -l "object.*Mapper" across all 11 files
    - grep -q "MusicBrainzMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
    - grep -q "LastFmMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
    - grep -q "DeezerMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerProvider.kt
    - grep -q "ITunesMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesProvider.kt
    - grep -q "DiscogsMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
    - grep -q "CoverArtArchiveMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt
    - grep -q "FanartTvMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt
    - grep -q "WikidataMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
    - grep -q "WikipediaMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikipedia/WikipediaProvider.kt
    - grep -q "ListenBrainzMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/listenbrainz/ListenBrainzProvider.kt
    - grep -q "LrcLibMapper\." musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lrclib/LrcLibProvider.kt
    - All tests pass
  </acceptance_criteria>
  <done>All 11 providers delegate EnrichmentData construction to their corresponding mapper object. No provider constructs EnrichmentData subclasses inline. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ApiKeyConfig and Builder.withDefaultProviders()</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/http/DefaultHttpClient.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/http/RateLimiter.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/engine/DefaultEnrichmentEngineTest.kt
  </read_first>
  <behavior>
    - Test: Builder.withDefaultProviders() without any API keys creates 8 providers (all except LastFm, FanartTv, Discogs which require keys)
    - Test: Builder.withDefaultProviders() with all API keys creates 11 providers
    - Test: Builder.apiKeys(ApiKeyConfig(lastFmKey = "key")) makes LastFm available via withDefaultProviders()
    - Test: withDefaultProviders() providers are correctly typed (MusicBrainz is identity, CoverArtArchive requires MBID, etc.)
  </behavior>
  <action>
1. In `EnrichmentConfig.kt`, add `ApiKeyConfig` data class after the existing `EnrichmentConfig`:

```kotlin
/**
 * Centralized API key configuration for all providers that need keys.
 * Pass to [EnrichmentEngine.Builder.apiKeys] to enable key-requiring providers
 * when using [EnrichmentEngine.Builder.withDefaultProviders].
 */
data class ApiKeyConfig(
    val lastFmKey: String? = null,
    val fanartTvProjectKey: String? = null,
    val discogsPersonalToken: String? = null,
)
```

2. In `EnrichmentEngine.kt`, add to the Builder class:

```kotlin
private var apiKeyConfig: ApiKeyConfig? = null

fun apiKeys(config: ApiKeyConfig) = apply { this.apiKeyConfig = config }

fun withDefaultProviders() = apply {
    val client = httpClient ?: DefaultHttpClient(config.userAgent)
    val mbRateLimiter = RateLimiter(1100)  // MusicBrainz: max 1 req/sec
    val defaultRateLimiter = RateLimiter(100)

    // Always-available providers (no API key needed)
    addProvider(MusicBrainzProvider(client, mbRateLimiter))
    addProvider(CoverArtArchiveProvider(client, defaultRateLimiter))
    addProvider(WikidataProvider(client, defaultRateLimiter))
    addProvider(WikipediaProvider(client, defaultRateLimiter))
    addProvider(DeezerProvider(client))
    addProvider(ITunesProvider(client))
    addProvider(ListenBrainzProvider(client, defaultRateLimiter))
    addProvider(LrcLibProvider(client, defaultRateLimiter))

    // Key-requiring providers (only added if key is provided)
    val keys = apiKeyConfig
    if (keys != null) {
        keys.lastFmKey?.let { addProvider(LastFmProvider(it, client, defaultRateLimiter)) }
        keys.fanartTvProjectKey?.let { addProvider(FanartTvProvider(it, client, defaultRateLimiter)) }
        keys.discogsPersonalToken?.let { addProvider(DiscogsProvider(it, client, defaultRateLimiter)) }
    }
}
```

3. Add required imports to `EnrichmentEngine.kt`:
```kotlin
import com.landofoz.musicmeta.http.RateLimiter
import com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzProvider
import com.landofoz.musicmeta.provider.coverartarchive.CoverArtArchiveProvider
import com.landofoz.musicmeta.provider.wikidata.WikidataProvider
import com.landofoz.musicmeta.provider.wikipedia.WikipediaProvider
import com.landofoz.musicmeta.provider.deezer.DeezerProvider
import com.landofoz.musicmeta.provider.itunes.ITunesProvider
import com.landofoz.musicmeta.provider.listenbrainz.ListenBrainzProvider
import com.landofoz.musicmeta.provider.lrclib.LrcLibProvider
import com.landofoz.musicmeta.provider.lastfm.LastFmProvider
import com.landofoz.musicmeta.provider.fanarttv.FanartTvProvider
import com.landofoz.musicmeta.provider.discogs.DiscogsProvider
```

4. In `build()`, if `httpClient` was not explicitly set, use the one that `withDefaultProviders()` might have already created. Actually, `withDefaultProviders()` creates its own `client` locally. The `build()` method still creates its own `DefaultHttpClient` for the engine. This is fine -- the engine's httpClient is used for other things (not provider HTTP calls; each provider has its own via its Api class). No change needed to `build()`.

5. Write tests in `DefaultEnrichmentEngineTest.kt` (or a new test file) that verify the behavior block. Use the `getProviders()` method to check how many providers are registered and their IDs/capabilities.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test -x :musicmeta-android:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "data class ApiKeyConfig" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
    - grep -q "lastFmKey" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
    - grep -q "fanartTvProjectKey" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
    - grep -q "discogsPersonalToken" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
    - grep -q "fun apiKeys" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt
    - grep -q "fun withDefaultProviders" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt
    - grep -q "MusicBrainzProvider" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt (imports for default providers)
    - All tests pass
  </acceptance_criteria>
  <done>ApiKeyConfig data class exists with keys for LastFm, FanartTv, and Discogs. Builder.apiKeys() stores the config. Builder.withDefaultProviders() creates all 8 keyless providers always, and adds key-requiring providers when keys are provided. Tests verify provider counts and availability.</done>
</task>

</tasks>

<verification>
Full test suite:
```bash
cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test -x :musicmeta-android:test
```

Verify all 11 mappers exist:
```bash
find musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider -name "*Mapper.kt" | wc -l
```
Should output 11.

Verify no provider constructs EnrichmentData inline (approximate check -- providers should reference their mapper):
```bash
grep -rn "EnrichmentData\.\(Artwork\|Metadata\|Lyrics\|Biography\|SimilarArtists\|Popularity\)(" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/*/\*Provider.kt
```
Should return zero matches (all EnrichmentData construction moved to mappers).

Verify ApiKeyConfig:
```bash
grep "data class ApiKeyConfig" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentConfig.kt
```

Verify withDefaultProviders:
```bash
grep "fun withDefaultProviders" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentEngine.kt
```
</verification>

<success_criteria>
- 11 *Mapper.kt files exist, one per provider
- No provider constructs EnrichmentData subclasses inline -- all delegate to mappers
- ApiKeyConfig data class exists with lastFmKey, fanartTvProjectKey, discogsPersonalToken
- Builder.apiKeys(ApiKeyConfig) stores the config
- Builder.withDefaultProviders() creates all providers, using keys from ApiKeyConfig when available
- All unit tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-abstraction/02-03-SUMMARY.md`
</output>
