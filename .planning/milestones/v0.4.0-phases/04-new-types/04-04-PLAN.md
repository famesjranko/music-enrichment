---
phase: 04-new-types
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProviderTest.kt
autonomous: true
requirements:
  - TYPE-01
  - TYPE-07
  - TYPE-08

must_haves:
  truths:
    - "Artwork results from Cover Art Archive include populated sizes list with multiple size entries"
    - "Artwork results from Deezer include sizes list with small, medium, big, xl entries"
    - "Artwork results from iTunes include sizes list with multiple generated sizes"
    - "Artwork results from Fanart.tv include sizes list"
    - "WikidataProvider returns birth/death dates, country, and occupation from expanded properties"
    - "Discogs provider declares BAND_MEMBERS capability and returns band members from artist endpoint"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt"
      provides: "getArtworkMetadata() JSON endpoint for sizes"
      contains: "getArtworkMetadata"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt"
      provides: "getEntityProperties() fetching P18, P569, P570, P495, P106"
      contains: "P569"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt"
      provides: "getArtist() endpoint for band members"
      contains: "getArtist"
  key_links:
    - from: "CoverArtArchiveMapper.kt"
      to: "EnrichmentData.kt"
      via: "sizes list on Artwork"
      pattern: "ArtworkSize"
    - from: "WikidataProvider.kt"
      to: "WikidataApi.kt"
      via: "getEntityProperties for metadata + image"
      pattern: "getEntityProperties|P569"
    - from: "DiscogsProvider.kt"
      to: "DiscogsApi.kt"
      via: "getArtist for band members"
      pattern: "getArtist"
---

<objective>
Enhance artwork with sizes for all 4 artwork providers, expand Wikidata properties for richer metadata, and add Discogs band members capability.

Purpose: Artwork sizes give consumers choice of resolution. Wikidata expansion enriches Metadata with birth/death dates, country, and occupation. Discogs band members provides a fallback source for TYPE-01.
Output: All artwork providers emit sizes, Wikidata returns expanded properties, Discogs returns band members.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-new-types/04-01-SUMMARY.md
@.planning/phases/04-new-types/04-03-SUMMARY.md
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt

<interfaces>
<!-- Key types from Plan 01 -->

From EnrichmentData.kt (created in Plan 01):
```kotlin
data class ArtworkSize(val url: String, val width: Int? = null, val height: Int? = null, val label: String? = null)

data class Artwork(
    val url: String,
    val width: Int? = null,
    val height: Int? = null,
    val thumbnailUrl: String? = null,
    val sizes: List<ArtworkSize>? = null,
) : EnrichmentData()

data class BandMembers(val members: List<BandMember>) : EnrichmentData()
data class BandMember(val name: String, val role: String? = null, val activePeriod: String? = null, val identifiers: EnrichmentIdentifiers = EnrichmentIdentifiers())
```

From Plan 03 (Fanart.tv already has ARTIST_BANNER capability):
```kotlin
// FanartTvProvider now routes ARTIST_BANNER in enrichFromImages()
// FanartTvArtistImages.banners is already used
```

Existing CoverArtArchiveApi:
```kotlin
suspend fun getArtworkUrl(releaseId: String, size: Int = 1200): String?
```

Existing DeezerAlbumResult:
```kotlin
data class DeezerAlbumResult(val id: Long, val title: String, val artistName: String,
    val coverSmall: String?, val coverMedium: String?, val coverBig: String?, val coverXl: String?)
```

Existing WikidataApi:
```kotlin
suspend fun getArtistImageUrl(wikidataId: String, size: Int): String?
```

Existing Metadata:
```kotlin
data class Metadata(val genres: List<String>?, val label: String?, val releaseDate: String?,
    val releaseType: String?, val country: String?, val barcode: String?, val disambiguation: String?,
    val artistType: String?, val beginDate: String?, val endDate: String?, val isrc: String?)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Artwork sizes for Cover Art Archive, Deezer, iTunes, and Fanart.tv</name>
  <read_first>
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProviderTest.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProviderTest.kt
  </read_first>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvProviderTest.kt
  </files>
  <action>
**Cover Art Archive (needs new JSON endpoint):**

CoverArtArchiveApi.kt - The current approach uses redirect URLs (`front-{size}`). For sizes, we need the JSON metadata endpoint which lists all images with their thumbnails. Add:

`getArtworkMetadata(releaseId: String): CoverArtArchiveImageList?` - Calls `GET https://coverartarchive.org/release/{releaseId}` (no `/front-*` suffix). This returns JSON with an `images[]` array. Parse it to extract front cover images with their `thumbnails` map (which has keys like "250", "500", "1200" or "small", "large"). Return a simple data holder:

```kotlin
data class CoverArtArchiveImageList(val images: List<CoverArtArchiveImage>)
data class CoverArtArchiveImage(val front: Boolean, val url: String, val thumbnails: Map<String, String>)
```

Parse logic: `json.optJSONArray("images")` -> for each image object, extract `front` (boolean), `image` (full URL), and `thumbnails` (JSONObject -> entries as Map). Filter for `front == true`, take the first.

CoverArtArchiveMapper.kt - Update `toArtwork` to accept optional `CoverArtArchiveImage`:

```kotlin
fun toArtwork(url: String, thumbnailUrl: String?, image: CoverArtArchiveImage? = null): EnrichmentData.Artwork {
    val sizes = image?.thumbnails?.map { (label, sizeUrl) ->
        ArtworkSize(url = sizeUrl, label = label)
    }
    return EnrichmentData.Artwork(url = url, thumbnailUrl = thumbnailUrl, sizes = sizes)
}
```

CoverArtArchiveProvider.kt - In `findArtwork()`, after getting the redirect URL for the main image, also call `api.getArtworkMetadata(releaseId)` to get the sizes. Find the first front image from the metadata. Pass it to the mapper. If metadata fetch fails (null), still return artwork without sizes (graceful degradation).

**Deezer:**

DeezerMapper.kt - Update the existing `toArtwork()` method to populate sizes from the 4 cover URLs. Note: Plan 03 also added `toDiscography()` and `toTracklist()` methods to this file -- those remain unchanged.

```kotlin
fun toArtwork(result: DeezerAlbumResult): EnrichmentData.Artwork? {
    val url = result.coverXl ?: result.coverBig ?: result.coverMedium ?: result.coverSmall ?: return null
    val sizes = listOfNotNull(
        result.coverSmall?.let { ArtworkSize(url = it, width = 56, height = 56, label = "small") },
        result.coverMedium?.let { ArtworkSize(url = it, width = 250, height = 250, label = "medium") },
        result.coverBig?.let { ArtworkSize(url = it, width = 500, height = 500, label = "big") },
        result.coverXl?.let { ArtworkSize(url = it, width = 1000, height = 1000, label = "xl") },
    )
    return EnrichmentData.Artwork(url = url, thumbnailUrl = result.coverMedium, sizes = sizes.takeIf { it.isNotEmpty() })
}
```

**iTunes:**

ITunesMapper.kt - Update `toArtwork()` to generate sizes from the URL template:

```kotlin
fun toArtwork(result: ITunesAlbumResult, artworkSize: Int): EnrichmentData.Artwork? {
    val artworkUrl = result.artworkUrl ?: return null
    val highResUrl = artworkUrl.replace("100x100bb", "${artworkSize}x${artworkSize}bb")
    val sizes = listOf(250, 500, 1000, 3000).map { size ->
        ArtworkSize(
            url = artworkUrl.replace("100x100bb", "${size}x${size}bb"),
            width = size,
            height = size,
            label = "${size}px",
        )
    }
    return EnrichmentData.Artwork(url = highResUrl, thumbnailUrl = artworkUrl, sizes = sizes)
}
```

**Fanart.tv:**

NOTE: Plan 03 already added ARTIST_BANNER capability to FanartTvProvider. This task modifies the underlying models to carry richer image data for sizes.

FanartTvModels.kt - Update `FanartTvArtistImages` to store full image objects instead of just URL strings. Change each list from `List<String>` to `List<FanartTvImage>`:

```kotlin
data class FanartTvImage(val url: String, val id: String? = null, val likes: Int = 0)

data class FanartTvArtistImages(
    val thumbnails: List<FanartTvImage>,
    val backgrounds: List<FanartTvImage>,
    val logos: List<FanartTvImage>,
    val banners: List<FanartTvImage>,
    val albumCovers: List<FanartTvImage> = emptyList(),
    val cdArt: List<FanartTvImage> = emptyList(),
)
```

FanartTvApi.kt - Update `parseArtistImages()` and `extractUrls()`:
- Rename `extractUrls` to `extractImages` returning `List<FanartTvImage>`. Parse each entry for `url`, `id`, `likes`.
- Update all callers.

FanartTvProvider.kt - Update `enrichFromImages()`: the `when` branches now get `List<FanartTvImage>` instead of `List<String>`. Get the first image, then pass it plus the full list to the mapper:

```kotlin
private fun enrichFromImages(images: FanartTvArtistImages, type: EnrichmentType): EnrichmentResult {
    val imageList = when (type) {
        EnrichmentType.ARTIST_PHOTO -> images.thumbnails
        EnrichmentType.ARTIST_BACKGROUND -> images.backgrounds
        EnrichmentType.ARTIST_LOGO -> images.logos
        EnrichmentType.ARTIST_BANNER -> images.banners
        EnrichmentType.ALBUM_ART -> images.albumCovers
        EnrichmentType.CD_ART -> images.cdArt
        else -> null
    } ?: return EnrichmentResult.NotFound(type, id)
    val image = imageList.firstOrNull() ?: return EnrichmentResult.NotFound(type, id)
    return success(FanartTvMapper.toArtwork(image, imageList), type)
}
```

FanartTvMapper.kt - Update `toArtwork` to accept `FanartTvImage` and optional all-images list:

```kotlin
fun toArtwork(image: FanartTvImage, allImages: List<FanartTvImage> = emptyList()): EnrichmentData.Artwork {
    val sizes = allImages.map { img ->
        ArtworkSize(url = img.url, label = img.id)
    }.takeIf { it.size > 1 }
    return EnrichmentData.Artwork(url = image.url, sizes = sizes)
}
```

**Tests:**

CoverArtArchiveProviderTest.kt - Add test:
`enrich returns artwork with sizes from metadata endpoint` - Setup fake JSON response for `coverartarchive.org/release/{id}` (without `/front-*` suffix) containing `images[]` array with front image and thumbnails map. Setup redirect response for the front-size URL. Verify the returned Artwork has non-null sizes list with entries.

FanartTvProviderTest.kt - Update existing tests: The JSON fixture format is the same (objects with `url` field), but verify the internal model change works. Add a test that verifies artwork includes sizes when multiple images exist for a type.
  </action>
  <acceptance_criteria>
    - grep "getArtworkMetadata" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveApi.kt
    - grep "ArtworkSize" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveMapper.kt
    - grep "ArtworkSize" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/deezer/DeezerMapper.kt
    - grep "ArtworkSize" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/itunes/ITunesMapper.kt
    - grep "FanartTvImage" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/fanarttv/FanartTvModels.kt
    - grep "sizes" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/coverartarchive/CoverArtArchiveProviderTest.kt
  </acceptance_criteria>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.coverartarchive.*" --tests "com.landofoz.musicmeta.provider.deezer.*" --tests "com.landofoz.musicmeta.provider.itunes.*" --tests "com.landofoz.musicmeta.provider.fanarttv.*" 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 artwork providers (CAA, Deezer, iTunes, Fanart.tv) populate sizes on Artwork. CAA uses JSON metadata endpoint. Deezer uses 4 cover URLs. iTunes generates from URL template. Fanart.tv passes all image variants. All existing and new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wikidata expanded properties and Discogs band members</name>
  <read_first>
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
    - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProviderTest.kt
    - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  </read_first>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProviderTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  </files>
  <action>
**Wikidata expanded properties (TYPE-08):**

WikidataApi.kt - Add a broader method alongside the existing one:

`getEntityProperties(wikidataId: String, imageSize: Int = DEFAULT_IMAGE_SIZE): WikidataEntityProperties?`:
- Calls `GET /w/api.php?action=wbgetclaims&entity={id}&property=P18|P569|P570|P495|P106&format=json`.
  The Wikidata API allows multiple properties separated by `|` in a single call.
- Returns a data class (define in WikidataApi.kt or a separate file):

```kotlin
data class WikidataEntityProperties(
    val imageUrl: String?,
    val birthDate: String?,
    val deathDate: String?,
    val countryOfOrigin: String?,
    val occupation: String?,
)
```

Parse each property from the `claims` object:
- P18 (image): Reuse existing `extractImageFilename()` + `buildCommonsUrl()` logic.
- P569/P570 (time values): Extract `mainsnak.datavalue.value.time`. The value looks like `+1968-10-07T00:00:00Z`. Strip leading `+`, take substring before `T` to get `1968-10-07`.
- P495 (entity ID for country): Extract `mainsnak.datavalue.value.id` (e.g., `Q30`). Map common QIDs to country names using a companion map: `Q30` -> `US`, `Q145` -> `UK`, `Q142` -> `France`, `Q183` -> `Germany`, `Q17` -> `Japan`, `Q38` -> `Italy`, `Q29` -> `Spain`, `Q16` -> `Canada`, `Q408` -> `Australia`, `Q36` -> `Poland`, `Q159` -> `Russia`, `Q211` -> `Czech Republic`, `Q31` -> `Belgium`, `Q55` -> `Netherlands`. Fall back to QID string if not in map.
- P106 (entity ID for occupation): Extract `mainsnak.datavalue.value.id`. Map common QIDs: `Q177220` -> `singer`, `Q639669` -> `musician`, `Q36834` -> `composer`, `Q488205` -> `singer-songwriter`, `Q183945` -> `record producer`. Fall back to QID.

Apply preferred-rank filtering to ALL properties (same logic as existing P18 handling).

Update existing `getArtistImageUrl()` to delegate: `return getEntityProperties(wikidataId, size)?.imageUrl`.

WikidataMapper.kt - Add method:

```kotlin
fun toMetadata(props: WikidataEntityProperties): EnrichmentData.Metadata =
    EnrichmentData.Metadata(
        beginDate = props.birthDate,
        endDate = props.deathDate,
        country = props.countryOfOrigin,
        artistType = props.occupation,
    )
```

WikidataProvider.kt:

1. Add COUNTRY capability (priority 50, fallback behind MusicBrainz at 100):
```kotlin
ProviderCapability(
    type = EnrichmentType.COUNTRY,
    priority = 50,
    identifierRequirement = IdentifierRequirement.WIKIDATA_ID,
),
```

2. Refactor `enrich()` to call `getEntityProperties()` and route by type:
```kotlin
override suspend fun enrich(request: EnrichmentRequest, type: EnrichmentType): EnrichmentResult {
    val wikidataId = request.identifiers.wikidataId
    if (wikidataId.isNullOrBlank()) return EnrichmentResult.NotFound(type, id)

    val props = api.getEntityProperties(wikidataId, imageSize)
        ?: return EnrichmentResult.NotFound(type, id)

    return when (type) {
        EnrichmentType.ARTIST_PHOTO -> {
            val imageUrl = props.imageUrl ?: return EnrichmentResult.NotFound(type, id)
            EnrichmentResult.Success(type, WikidataMapper.toArtwork(imageUrl), id, CONFIDENCE)
        }
        EnrichmentType.COUNTRY -> {
            val metadata = WikidataMapper.toMetadata(props)
            if (metadata.country == null && metadata.beginDate == null && metadata.endDate == null && metadata.artistType == null) {
                return EnrichmentResult.NotFound(type, id)
            }
            EnrichmentResult.Success(type, metadata, id, CONFIDENCE)
        }
        else -> EnrichmentResult.NotFound(type, id)
    }
}
```

**Discogs band members (TYPE-01):**

DiscogsModels.kt - Add:
```kotlin
data class DiscogsArtist(
    val id: Long,
    val name: String,
    val members: List<DiscogsMember>,
)

data class DiscogsMember(
    val id: Long,
    val name: String,
    val active: Boolean?,
)
```

DiscogsApi.kt - Add 2 new methods:

`getArtist(artistId: Long): DiscogsArtist?` - Calls `GET https://api.discogs.com/artists/{id}?token={token}`. Parses `id`, `name`, and `members[]` array (each has `id`, `name`, `active`). Returns null on error/404.

`searchArtist(name: String): Long?` - Calls `GET https://api.discogs.com/database/search?type=artist&q={name}&per_page=1&token={token}`. Returns first result `id`, or null.

DiscogsMapper.kt - Add:
```kotlin
fun toBandMembers(artist: DiscogsArtist): EnrichmentData.BandMembers =
    EnrichmentData.BandMembers(
        members = artist.members.map { member ->
            BandMember(
                name = member.name,
                identifiers = EnrichmentIdentifiers().withExtra("discogsArtistId", member.id.toString()),
            )
        }
    )
```

DiscogsProvider.kt:
1. Add capability: `ProviderCapability(EnrichmentType.BAND_MEMBERS, priority = 50)`

2. Update `enrich()` to handle ForArtist requests for BAND_MEMBERS:
```kotlin
if (type == EnrichmentType.BAND_MEMBERS) {
    val artistRequest = request as? EnrichmentRequest.ForArtist
        ?: return EnrichmentResult.NotFound(type, id)
    return enrichBandMembers(artistRequest, type)
}
// ... existing ForAlbum logic
```

Add `enrichBandMembers(request, type)`:
- Search artist: `api.searchArtist(request.name)` -> get Discogs artist ID
- If null, return NotFound
- Lookup artist: `api.getArtist(artistId)`
- If null or members empty, return NotFound
- Return Success with `DiscogsMapper.toBandMembers(artist)`, confidence = CONFIDENCE

**Tests:**

WikidataProviderTest.kt - Add 2 tests:
1. `enrich returns Metadata with birth and death dates from Wikidata properties` - JSON fixture with claims containing P569 (time: `+1968-10-07T00:00:00Z`), P570, P495 (entity: `Q30`). Request COUNTRY type. Verify Metadata has beginDate=`1968-10-07`, country=`US`.
2. `enrich returns NotFound for COUNTRY when no properties present` - JSON with empty claims object. Verify NotFound.

DiscogsProviderTest.kt - Add 2 tests:
1. `enrich returns BandMembers for artist` - JSON fixture for search (returns id=12345) and artist detail (returns members list with 2 members). Verify Success with BandMembers containing 2 members.
2. `enrich returns NotFound for BandMembers when artist has no members` - Artist response has empty/missing members. Verify NotFound.
  </action>
  <acceptance_criteria>
    - grep "getEntityProperties" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt
    - grep "P569" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataApi.kt
    - grep "toMetadata" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataMapper.kt
    - grep "COUNTRY" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProvider.kt
    - grep "getArtist" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
    - grep "searchArtist" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
    - grep "BAND_MEMBERS" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProvider.kt
    - grep "toBandMembers" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
    - grep "birth" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/wikidata/WikidataProviderTest.kt
    - grep "BandMembers" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  </acceptance_criteria>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.wikidata.*" --tests "com.landofoz.musicmeta.provider.discogs.*" 2>&1 | tail -10</automated>
  </verify>
  <done>Wikidata returns birth/death dates, country, occupation from expanded properties via single API call. Discogs returns band members from artist endpoint. All tests pass.</done>
</task>

</tasks>

<verification>
- `./gradlew :musicmeta-core:test` passes (all tests including updated artwork providers, Wikidata, Discogs)
- Artwork from CAA, Deezer, iTunes, Fanart.tv includes sizes
- Wikidata enriches COUNTRY with metadata including birth/death dates
- Discogs enriches BAND_MEMBERS
</verification>

<success_criteria>
- All 4 artwork providers return Artwork with populated sizes list (at least 2 entries)
- WikidataProvider returns Metadata with birth/death dates, country, and occupation from Wikidata properties
- DiscogsProvider returns BandMembers from artist endpoint
- All existing tests still pass with updated models
</success_criteria>

<output>
After completion, create `.planning/phases/04-new-types/04-04-SUMMARY.md`
</output>
