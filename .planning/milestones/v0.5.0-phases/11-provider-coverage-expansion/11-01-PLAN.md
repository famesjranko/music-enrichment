---
phase: 11-provider-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapperTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
autonomous: true
requirements: [PROV-01, PROV-05]

must_haves:
  truths:
    - "Last.fm album.getinfo appears in the ALBUM_METADATA provider chain at priority 40"
    - "Last.fm album metadata returns title, artist, playcount, listeners, tags, and wiki summary"
    - "Discogs release details include community rating average, rating count, have count, and want count"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt"
      provides: "getAlbumInfo API method"
      contains: "fun getAlbumInfo"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt"
      provides: "LastFmAlbumInfo data class"
      contains: "data class LastFmAlbumInfo"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt"
      provides: "toAlbumMetadata mapper"
      contains: "fun toAlbumMetadata"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt"
      provides: "ALBUM_METADATA capability at priority 40"
      contains: "ALBUM_METADATA"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt"
      provides: "Community rating fields on DiscogsReleaseDetail"
      contains: "communityRating"
  key_links:
    - from: "LastFmProvider"
      to: "LastFmApi.getAlbumInfo"
      via: "enrichAlbumMetadata calls api.getAlbumInfo"
      pattern: "api\\.getAlbumInfo"
    - from: "LastFmProvider"
      to: "LastFmMapper.toAlbumMetadata"
      via: "enrichAlbumMetadata maps result"
      pattern: "LastFmMapper\\.toAlbumMetadata"
    - from: "DiscogsMapper.toAlbumMetadata"
      to: "DiscogsReleaseDetail"
      via: "extracts community data from release details"
      pattern: "communityRating"
---

<objective>
Add Last.fm album.getinfo as an ALBUM_METADATA source at priority 40 and deepen Discogs release details with community rating/collector data.

Purpose: Fill the ALBUM_METADATA gap where only MusicBrainz (100), Deezer (50), Discogs (40), and iTunes (30) existed. Last.fm adds playcount, listeners, wiki summary, and tags. Discogs community data (rating, have/want counts) enriches album metadata at no extra API cost since getReleaseDetails() already exists from Phase 7.

Output: Last.fm ALBUM_METADATA capability registered. Discogs community data extracted from existing API call. Both with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapperTest.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
@musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentProvider.kt
@musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/testutil/FakeHttpClient.kt

<interfaces>
<!-- Key types the executor needs -->

From EnrichmentData.kt:
```kotlin
@Serializable
data class Metadata(
    val genres: List<String>? = null,
    val genreTags: List<GenreTag>? = null,
    val label: String? = null,
    val releaseDate: String? = null,
    val releaseType: String? = null,
    val country: String? = null,
    val barcode: String? = null,
    val disambiguation: String? = null,
    val artistType: String? = null,
    val beginDate: String? = null,
    val endDate: String? = null,
    val isrc: String? = null,
    val trackCount: Int? = null,
    val explicit: Boolean? = null,
    val catalogNumber: String? = null,
    val communityRating: Float? = null,
) : EnrichmentData()
```

From ConfidenceCalculator.kt:
```kotlin
object ConfidenceCalculator {
    fun fuzzyMatch(hasArtistMatch: Boolean): Float // 0.8 or 0.6
}
```

From ProviderCapability:
```kotlin
data class ProviderCapability(
    val type: EnrichmentType,
    val priority: Int,
    val identifierRequirement: IdentifierRequirement = IdentifierRequirement.NONE,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Last.fm album.getinfo API, model, mapper, and provider capability (PROV-01)</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  </read_first>
  <behavior>
    - Test: `enrich returns album metadata from album.getinfo` -- ForAlbum("OK Computer", "Radiohead") with valid album.getinfo JSON returns Success with Metadata containing playcount listeners in summary field, genres from tags, trackCount
    - Test: `enrich returns NotFound for ALBUM_METADATA when album not found` -- album.getinfo returns error JSON, result is NotFound
    - Test: `enrich returns NotFound for ALBUM_METADATA with ForArtist request` -- ForArtist request returns NotFound (ALBUM_METADATA requires ForAlbum)
    - Test: `capabilities include ALBUM_METADATA at priority 40` -- verify capability exists with correct priority
    - Test: `enrich returns album metadata with genreTags` -- tags from album.getinfo mapped to GenreTag with confidence 0.3f and source "lastfm"
  </behavior>
  <action>
    Per PROV-01: Add Last.fm album.getinfo endpoint as ALBUM_METADATA source at priority 40.

    1. **LastFmModels.kt** -- Add `LastFmAlbumInfo` data class:
       ```kotlin
       data class LastFmAlbumInfo(
           val name: String,
           val artist: String,
           val playcount: Long?,
           val listeners: Long?,
           val tags: List<String>,
           val wiki: String?,
           val trackCount: Int?,
       )
       ```

    2. **LastFmApi.kt** -- Add `getAlbumInfo(album: String, artist: String): LastFmAlbumInfo?` method:
       - URL: `$BASE_URL?method=album.getinfo&album={encoded}&artist={encoded}&api_key={key}&format=json`
       - Add private `buildAlbumUrl(method: String, album: String, artist: String): String` helper
       - Parse from JSON: `json.optJSONObject("album")` -- extract name, artist from nested artist string, playcount/listeners from optString->toLongOrNull, tags from `album.tags.tag[]` array of objects with `name` field, wiki from `album.optJSONObject("wiki")?.optString("summary")`, trackCount from `album.optJSONArray("tracks")?.optJSONArray("track")?.length()`

    3. **LastFmMapper.kt** -- Add `toAlbumMetadata(info: LastFmAlbumInfo): EnrichmentData.Metadata`:
       - Map tags to `genres` list and `genreTags` list with confidence 0.3f and source "lastfm" (same as existing toGenre pattern)
       - Map wiki to a summary-like field -- store in `disambiguation` field since Metadata has no `summary` field, or just include it in genres. Actually Metadata does not have a summary field, so the wiki data enriches the genre/tag side. Map `playcount` and `listeners` through -- these are not on Metadata. Since EnrichmentData.Metadata doesn't carry playcount/listeners, the primary value is genres/genreTags from album-level tags (which may differ from artist-level tags) plus trackCount.
       - Set `trackCount` from info.trackCount, `genres` from info.tags, `genreTags` from info.tags mapped to GenreTag(name, 0.3f, listOf("lastfm"))

    4. **LastFmProvider.kt** -- Add ALBUM_METADATA capability and routing:
       - Add to capabilities: `ProviderCapability(EnrichmentType.ALBUM_METADATA, priority = 40)`
       - In `enrich()`, add a guard before the ForArtist cast: if `type == EnrichmentType.ALBUM_METADATA`, cast request to `ForAlbum`, call `enrichAlbumMetadata(albumRequest, type)`, wrap in try/catch with mapError
       - Add private `enrichAlbumMetadata(request: EnrichmentRequest.ForAlbum, type: EnrichmentType): EnrichmentResult` that calls `api.getAlbumInfo(request.title, request.artist)`, returns NotFound if null, maps via `LastFmMapper.toAlbumMetadata(info)`, returns Success with `ConfidenceCalculator.fuzzyMatch(hasArtistMatch = true)` confidence

    5. **LastFmProviderTest.kt** -- Add tests per behavior block above. Use FakeHttpClient.givenJsonResponse with URL key "album.getinfo" and realistic album.getinfo JSON fixture.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.lastfm.LastFmProviderTest" -x :musicmeta-android:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fun getAlbumInfo" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmApi.kt
    - grep -q "data class LastFmAlbumInfo" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmModels.kt
    - grep -q "fun toAlbumMetadata" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmMapper.kt
    - grep -q "ALBUM_METADATA" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
    - grep -q "priority = 40" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
    - grep -q "enrichAlbumMetadata" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProvider.kt
    - grep -q "album metadata" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/lastfm/LastFmProviderTest.kt
  </acceptance_criteria>
  <done>Last.fm album.getinfo returns ALBUM_METADATA with tags mapped to genreTags at confidence 0.3f, trackCount populated, capability registered at priority 40. All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discogs community rating and collector data from release details (PROV-05)</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapperTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapperTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsProviderTest.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  </read_first>
  <behavior>
    - Test: `toAlbumMetadata includes community rating from release detail` -- DiscogsReleaseDetail with communityRating=4.2, ratingCount=150, haveCount=5000, wantCount=1200 maps to Metadata with communityRating=4.2f
    - Test: `toAlbumMetadata handles null community data gracefully` -- DiscogsReleaseDetail with null community fields maps to Metadata with null communityRating
    - Test: `parseReleaseDetail extracts community rating fields` -- JSON with community.rating.average and community.have/want parsed correctly into DiscogsReleaseDetail
  </behavior>
  <action>
    Per PROV-05: Extract community data from the existing getReleaseDetails() response. No additional API calls needed -- piggybacks on Phase 7's credits endpoint.

    1. **DiscogsModels.kt** -- Extend `DiscogsReleaseDetail` with community fields:
       ```kotlin
       data class DiscogsReleaseDetail(
           val id: Long,
           val title: String,
           val extraartists: List<DiscogsCredit>,
           val tracklist: List<DiscogsTrackItem>,
           val communityRating: Float? = null,
           val ratingCount: Int? = null,
           val haveCount: Int? = null,
           val wantCount: Int? = null,
       )
       ```
       Use default values = null so existing callers (credits) are unaffected.

    2. **DiscogsApi.kt** -- Update `parseReleaseDetail()` to extract community data:
       - From JSON: `json.optJSONObject("community")` -> extract:
         - `community.optJSONObject("rating")?.optDouble("average")?.toFloat()` for communityRating (skip if 0.0)
         - `community.optJSONObject("rating")?.optInt("count", 0)` for ratingCount (skip if 0)
         - `community.optInt("have", 0)` for haveCount (skip if 0)
         - `community.optInt("want", 0)` for wantCount (skip if 0)
       - Use `.takeIf { it > 0 }` for counts and `.takeIf { it > 0f }` for rating

    3. **DiscogsMapper.kt** -- Add `toAlbumMetadataFromDetail(detail: DiscogsReleaseDetail): EnrichmentData.Metadata` or extend existing `toAlbumMetadata` to accept DiscogsReleaseDetail. Since the existing `toAlbumMetadata(release: DiscogsRelease)` takes a search result, add a new overload:
       ```kotlin
       fun toAlbumMetadataFromDetail(detail: DiscogsReleaseDetail): EnrichmentData.Metadata =
           EnrichmentData.Metadata(
               communityRating = detail.communityRating,
           )
       ```
       Actually, the simplest approach: the communityRating field already exists on Metadata. Just modify the existing `toAlbumMetadata(release: DiscogsRelease)` to NOT carry this (it comes from search results which don't have community). Instead, when DiscogsProvider enriches ALBUM_METADATA via search, also check if discogsReleaseId is available and fetch details for community data. But that would add an API call.

       Better approach per CONTEXT.md: When getReleaseDetails is already called for credits, the community data comes for free. So add community data to the DiscogsMapper but as a separate function `enrichWithCommunityData(detail: DiscogsReleaseDetail): EnrichmentData.Metadata` that returns a Metadata with just communityRating set. The existing ALBUM_METADATA path (via search) can optionally call getReleaseDetails if a discogsReleaseId is on identifiers.

       Simplest correct approach: Add to existing `toAlbumMetadata(release: DiscogsRelease)` nothing (search results don't have community). Instead, in DiscogsProvider, when enriching ALBUM_METADATA and a discogsReleaseId is available on identifiers.extra, fetch release details and include communityRating in the Metadata. If no discogsReleaseId, fall back to search-only metadata.

       In DiscogsProvider.enrich(), update the ALBUM_METADATA path: after `enrichFromRelease(release, type)`, if the release has a releaseId, call `api.getReleaseDetails(releaseId)` and merge the communityRating into the Metadata result. Keep it simple -- add a helper `enrichAlbumMetadataWithCommunity` that builds on existing search + optionally enriches with detail data.

    4. **DiscogsProviderTest.kt / DiscogsMapperTest.kt** -- Add tests per behavior block. Test that community data flows through from JSON to Metadata.communityRating.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.discogs.*" -x :musicmeta-android:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "communityRating" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
    - grep -q "haveCount" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
    - grep -q "wantCount" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsModels.kt
    - grep -q "communityRating" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsApi.kt
    - grep -q "communityRating" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapper.kt
    - grep -q "community" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/discogs/DiscogsMapperTest.kt
  </acceptance_criteria>
  <done>Discogs release details carry communityRating, ratingCount, haveCount, wantCount. DiscogsMapper includes community rating in ALBUM_METADATA results when available. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
```bash
# All Last.fm tests pass including new ALBUM_METADATA tests
./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.lastfm.*" -x :musicmeta-android:compileDebugKotlin

# All Discogs tests pass including new community data tests
./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.discogs.*" -x :musicmeta-android:compileDebugKotlin

# Full core test suite still passes
./gradlew :musicmeta-core:test -x :musicmeta-android:compileDebugKotlin
```
</verification>

<success_criteria>
1. Last.fm provider has ALBUM_METADATA capability at priority 40
2. Last.fm album.getinfo returns Metadata with genreTags and trackCount for ForAlbum requests
3. Discogs DiscogsReleaseDetail carries community rating, have/want counts
4. Discogs ALBUM_METADATA enrichment includes communityRating when release details are available
5. All existing tests continue to pass
6. New tests cover both happy path and error/empty cases
</success_criteria>

<output>
After completion, create `.planning/phases/11-provider-coverage-expansion/11-01-SUMMARY.md`
</output>
