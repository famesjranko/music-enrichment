---
phase: 08-release-editions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParser.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzApi.kt
  - musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParserTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
  - musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/e2e/EnrichmentShowcaseTest.kt
autonomous: true
requirements: [EDIT-01, EDIT-02]

must_haves:
  truths:
    - "RELEASE_EDITIONS enum value exists with 1-year TTL"
    - "ReleaseEditions and ReleaseEdition are @Serializable data classes in EnrichmentData.kt"
    - "MusicBrainz lookupReleaseGroup fetches releases from release-group endpoint"
    - "Parser extracts title, format, country, year, label, catalogNumber, barcode from release-group releases"
    - "MusicBrainz RELEASE_EDITIONS capability wired at priority 100 with MUSICBRAINZ_RELEASE_GROUP_ID requirement"
    - "enrichAlbumEditions returns Success with non-empty editions list when release-group has releases"
  artifacts:
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt"
      provides: "RELEASE_EDITIONS enum value"
      contains: "RELEASE_EDITIONS"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt"
      provides: "ReleaseEditions and ReleaseEdition data classes"
      contains: "ReleaseEditions"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzApi.kt"
      provides: "lookupReleaseGroup API method"
      contains: "lookupReleaseGroup"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParser.kt"
      provides: "parseReleaseGroupDetail parser"
      contains: "parseReleaseGroupDetail"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt"
      provides: "toReleaseEditions mapper"
      contains: "toReleaseEditions"
    - path: "musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt"
      provides: "RELEASE_EDITIONS capability and enrichAlbumEditions"
      contains: "enrichAlbumEditions"
  key_links:
    - from: "MusicBrainzProvider.kt"
      to: "MusicBrainzApi.lookupReleaseGroup"
      via: "enrichAlbumEditions calls api.lookupReleaseGroup"
      pattern: "api\\.lookupReleaseGroup"
    - from: "MusicBrainzProvider.kt"
      to: "MusicBrainzParser.parseReleaseGroupDetail"
      via: "API returns raw JSONObject, parser extracts editions"
      pattern: "parseReleaseGroupDetail"
    - from: "MusicBrainzProvider.kt"
      to: "MusicBrainzMapper.toReleaseEditions"
      via: "mapper converts DTOs to EnrichmentData.ReleaseEditions"
      pattern: "toReleaseEditions"
---

<objective>
Add RELEASE_EDITIONS enrichment type with MusicBrainz as primary provider (priority 100).

Purpose: Enable consumers to list all editions/pressings of an album by requesting a single enrichment type. MusicBrainz release-group releases endpoint provides comprehensive edition data including format, country, year, label, and catalog number.

Output: RELEASE_EDITIONS enum value, ReleaseEditions/ReleaseEdition data classes, MusicBrainz API/Parser/Mapper/Provider wiring with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-editions/08-CONTEXT.md
@.planning/phases/07-credits-personnel/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From EnrichmentType.kt (add new value in "Additional metadata" section after ALBUM_TRACKS):
```kotlin
enum class EnrichmentType(val defaultTtlMs: Long) {
    // ...existing values...
    ALBUM_TRACKS(365L * 24 * 60 * 60 * 1000),
    // RELEASE_EDITIONS goes here
}
```

From EnrichmentData.kt (add sealed subclass and top-level data class):
```kotlin
sealed class EnrichmentData {
    // ...existing subclasses...
    data class Credits(val credits: List<Credit>) : EnrichmentData()
    // ReleaseEditions goes here
}
// Top-level: Credit, ReleaseEdition, etc.
```

From EnrichmentIdentifiers:
```kotlin
data class EnrichmentIdentifiers(
    val musicBrainzId: String? = null,
    val musicBrainzReleaseGroupId: String? = null,
    // ...
    val extra: Map<String, String> = emptyMap(),
)
```

From IdentifierRequirement:
```kotlin
enum class IdentifierRequirement {
    NONE, MUSICBRAINZ_ID, MUSICBRAINZ_RELEASE_GROUP_ID,
    WIKIDATA_ID, WIKIPEDIA_TITLE, ANY_IDENTIFIER,
}
```

From MusicBrainzApi (lookupRecording pattern to follow for lookupReleaseGroup):
```kotlin
suspend fun lookupRecording(mbid: String): JSONObject? {
    val json = rateLimiter.execute {
        when (val r = httpClient.fetchJsonResult("$BASE_URL/recording/$mbid?fmt=json&inc=...")) {
            is HttpResult.Ok -> r.body
            else -> return@execute null
        }
    }
    return json
}
```

From MusicBrainzModels.kt (existing release-group DTO):
```kotlin
data class MusicBrainzReleaseGroup(
    val id: String, val title: String,
    val primaryType: String?, val firstReleaseDate: String?,
)
```

From MusicBrainzProvider.kt capabilities pattern:
```kotlin
override val capabilities: List<ProviderCapability> = listOf(
    ProviderCapability(EnrichmentType.CREDITS, priority = 100,
        identifierRequirement = IdentifierRequirement.MUSICBRAINZ_ID),
)
```

From MusicBrainzProvider.kt enrichAlbum dispatch pattern:
```kotlin
private suspend fun enrichAlbum(request: EnrichmentRequest.ForAlbum, type: EnrichmentType): EnrichmentResult {
    if (type == EnrichmentType.ALBUM_TRACKS) return enrichAlbumTracks(request)
    // ...
}
```

From ConfidenceCalculator:
```kotlin
object ConfidenceCalculator {
    fun idBasedLookup(): Float = 1.0f
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define RELEASE_EDITIONS type and data model + MusicBrainz API/Parser/Mapper</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParser.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParserTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/e2e/EnrichmentShowcaseTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzApi.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParser.kt,
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParserTest.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/e2e/EnrichmentShowcaseTest.kt
  </read_first>
  <behavior>
    - Test: parseReleaseGroupDetail parses releases array from release-group lookup JSON into MusicBrainzReleaseGroupDetail with list of MusicBrainzEdition
    - Test: parseReleaseGroupDetail extracts format from media[0].format, label from label-info[0].label.name, catalogNumber from label-info[0].catalog-number, barcode, country, date (year extracted as first 4 chars)
    - Test: parseReleaseGroupDetail returns empty releases list when releases array is absent
    - Test: toReleaseEditions maps MusicBrainzEdition list to EnrichmentData.ReleaseEditions with correct field mapping including identifiers with musicBrainzId
    - Test: ReleaseEditions round-trip serialization (serialize + deserialize produces equal object)
  </behavior>
  <action>
    1. EnrichmentType.kt: Add RELEASE_EDITIONS(365L * 24 * 60 * 60 * 1000) in the "Additional metadata" section after ALBUM_TRACKS (per CONTEXT.md: 1-year TTL since editions rarely change).

    2. EnrichmentData.kt: Add sealed subclass and top-level data class (per CONTEXT.md decisions):
       - Inner: `data class ReleaseEditions(val editions: List<ReleaseEdition>) : EnrichmentData()`
       - Top-level: `data class ReleaseEdition(val title: String, val format: String? = null, val country: String? = null, val year: Int? = null, val label: String? = null, val catalogNumber: String? = null, val barcode: String? = null, val identifiers: EnrichmentIdentifiers = EnrichmentIdentifiers())`
       - Both annotated @Serializable
       - IMPORTANT: Adding a new sealed subclass to EnrichmentData will break the exhaustive `when` in EnrichmentShowcaseTest.snippet(). Add a branch: `is EnrichmentData.ReleaseEditions -> "${data.editions.size} editions"`

    3. MusicBrainzModels.kt: Add DTOs:
       - `data class MusicBrainzReleaseGroupDetail(val id: String, val title: String, val releases: List<MusicBrainzEdition>)`
       - `data class MusicBrainzEdition(val id: String, val title: String, val date: String?, val country: String?, val barcode: String?, val format: String?, val label: String?, val catalogNumber: String?)`

    4. MusicBrainzApi.kt: Add `lookupReleaseGroup(releaseGroupMbid: String): JSONObject?` method following lookupRecording pattern. URL: `$BASE_URL/release-group/$releaseGroupMbid?fmt=json&inc=releases+labels+media`. Returns raw JSONObject (same pattern as lookupRecording — the releases sub-structure is complex and only needed here).

    5. MusicBrainzParser.kt: Add `parseReleaseGroupDetail(json: JSONObject): MusicBrainzReleaseGroupDetail` that:
       - Extracts id, title from top-level
       - Iterates releases[] array, for each release:
         - id, title from release object
         - date from release.date (nullable)
         - country from release.country (nullable)
         - barcode from release.barcode (nullable)
         - format from media[0].format (nullable; first medium's format)
         - label from label-info[0].label.name (nullable; first label)
         - catalogNumber from label-info[0].catalog-number (nullable)
       - Returns MusicBrainzReleaseGroupDetail with parsed releases

    6. MusicBrainzMapper.kt: Add `toReleaseEditions(detail: MusicBrainzReleaseGroupDetail): EnrichmentData.ReleaseEditions` that maps each MusicBrainzEdition to ReleaseEdition:
       - title = edition.title
       - format = edition.format
       - country = edition.country
       - year = edition.date?.take(4)?.toIntOrNull() (extract year from "YYYY-MM-DD" or "YYYY")
       - label = edition.label
       - catalogNumber = edition.catalogNumber
       - barcode = edition.barcode
       - identifiers = EnrichmentIdentifiers(musicBrainzId = edition.id)

    Write tests first (RED), then implement (GREEN). Commit after GREEN.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzParserTest" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RELEASE_EDITIONS" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentType.kt
    - grep -q "ReleaseEditions" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - grep -q "ReleaseEdition" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/EnrichmentData.kt
    - grep -q "MusicBrainzReleaseGroupDetail" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt
    - grep -q "MusicBrainzEdition" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzModels.kt
    - grep -q "lookupReleaseGroup" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzApi.kt
    - grep -q "parseReleaseGroupDetail" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzParser.kt
    - grep -q "toReleaseEditions" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzMapper.kt
    - grep -q "ReleaseEditions" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/e2e/EnrichmentShowcaseTest.kt
    - ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzParserTest" passes
  </acceptance_criteria>
  <done>RELEASE_EDITIONS type with 1-year TTL exists. ReleaseEditions/ReleaseEdition @Serializable data classes exist. MusicBrainz lookupReleaseGroup API, parseReleaseGroupDetail parser, and toReleaseEditions mapper all implemented and tested. EnrichmentShowcaseTest exhaustive when handles new type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire MusicBrainz RELEASE_EDITIONS capability into Provider</name>
  <files>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
  </files>
  <read_first>
    musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt,
    musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
  </read_first>
  <behavior>
    - Test: capabilities includes RELEASE_EDITIONS at priority 100 with MUSICBRAINZ_RELEASE_GROUP_ID requirement
    - Test: enrichAlbumEditions returns Success with ReleaseEditions data when release-group has releases (mock lookupReleaseGroup returning JSON with releases array)
    - Test: enrichAlbumEditions returns NotFound when release-group MBID is missing from identifiers
    - Test: enrichAlbumEditions returns NotFound when lookupReleaseGroup returns null (API failure)
    - Test: enrichAlbumEditions returns Error with NETWORK ErrorKind on IOException
  </behavior>
  <action>
    1. MusicBrainzProvider.kt capabilities: Add `ProviderCapability(EnrichmentType.RELEASE_EDITIONS, priority = 100, identifierRequirement = IdentifierRequirement.MUSICBRAINZ_RELEASE_GROUP_ID)` to the capabilities list. Per CONTEXT.md: priority 100, needs release-group MBID from identity resolution.

    2. MusicBrainzProvider.kt enrichAlbum: Add dispatch guard at top of enrichAlbum (same pattern as ALBUM_TRACKS): `if (type == EnrichmentType.RELEASE_EDITIONS) return enrichAlbumEditions(request)`. Place it right after the existing ALBUM_TRACKS guard.

    3. Add private suspend fun enrichAlbumEditions(request: EnrichmentRequest.ForAlbum): EnrichmentResult:
       - val type = EnrichmentType.RELEASE_EDITIONS
       - val releaseGroupMbid = request.identifiers.musicBrainzReleaseGroupId ?: return EnrichmentResult.NotFound(type, id)
       - val json = api.lookupReleaseGroup(releaseGroupMbid) ?: return EnrichmentResult.NotFound(type, id)
       - val detail = MusicBrainzParser.parseReleaseGroupDetail(json)
       - if (detail.releases.isEmpty()) return EnrichmentResult.NotFound(type, id)
       - return EnrichmentResult.Success(type = type, data = MusicBrainzMapper.toReleaseEditions(detail), provider = id, confidence = ConfidenceCalculator.idBasedLookup())

    Write tests first (RED), then implement (GREEN). Commit after GREEN.
  </action>
  <verify>
    <automated>cd /home/andy/music-enrichment && ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzProviderTest" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RELEASE_EDITIONS" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
    - grep -q "enrichAlbumEditions" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
    - grep -q "MUSICBRAINZ_RELEASE_GROUP_ID" musicmeta-core/src/main/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProvider.kt
    - grep -q "RELEASE_EDITIONS" musicmeta-core/src/test/kotlin/com/landofoz/musicmeta/provider/musicbrainz/MusicBrainzProviderTest.kt
    - ./gradlew :musicmeta-core:test --tests "com.landofoz.musicmeta.provider.musicbrainz.MusicBrainzProviderTest" passes
    - ./gradlew :musicmeta-core:test passes (all tests green)
  </acceptance_criteria>
  <done>MusicBrainz RELEASE_EDITIONS capability at priority 100 with MUSICBRAINZ_RELEASE_GROUP_ID requirement. enrichAlbumEditions dispatched from enrichAlbum, reads release-group MBID from identifiers, calls lookupReleaseGroup, parses and maps to ReleaseEditions. All tests pass including 5 new provider tests.</done>
</task>

</tasks>

<verification>
1. `./gradlew :musicmeta-core:test` — all tests pass (existing + new parser and provider tests)
2. grep confirms RELEASE_EDITIONS in EnrichmentType, ReleaseEditions/ReleaseEdition in EnrichmentData, lookupReleaseGroup in Api, parseReleaseGroupDetail in Parser, toReleaseEditions in Mapper, enrichAlbumEditions in Provider
3. RELEASE_EDITIONS capability has priority 100 and MUSICBRAINZ_RELEASE_GROUP_ID identifier requirement
</verification>

<success_criteria>
- RELEASE_EDITIONS enum value exists with 365-day TTL
- ReleaseEditions and ReleaseEdition are @Serializable data classes with fields: title, format, country, year, label, catalogNumber, barcode, identifiers
- MusicBrainz lookupReleaseGroup fetches /ws/2/release-group/{mbid}?inc=releases+labels+media
- Parser extracts editions with format from media[0].format, label from label-info[0].label.name
- Provider wired at priority 100 with MUSICBRAINZ_RELEASE_GROUP_ID requirement
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-release-editions/08-01-SUMMARY.md`
</output>
